# Python Package

This package contains immutable transport DTOs, a client protocol, trajectory
assembly, and reward-adapter interfaces. It intentionally has no game state,
card execution, legal-action generation, or rules repair. The native transport
remains an open decision.
