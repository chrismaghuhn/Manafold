# Synthetic Bundle Example

This directory demonstrates layout only. IDs prefixed with `example/` or `TBD-`
are synthetic and the bundle is not certified. Replace it only after exact V1
decks, authority snapshots, content rights, capabilities, and acceptance gates
are locked.
