manifest_version = "0.2.2"
status = "example-only"

[host]
os = "TBD"
kernel = "TBD"
cpu_model = "TBD"
physical_cores = 0
logical_cores = 0
memory_bytes = 0

[toolchain]
rustc = "1.85.1"
python = "3.13.5"
profile = "release"
target = "TBD"

[measurement]
warmup_iterations = 0
sample_iterations = 0
isolated_cpu_set = "TBD"
notes = "Replace before any performance acceptance claim."
