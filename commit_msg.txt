OD-008: Fix RNG contract per second review

- R1: Restore exact stream-kind contract (SyntheticM1=0x0001 only, 0x0000 reserved)
  - Removed Shuffle/Sampling from OD-008 scope
  - Canonical SyntheticM1/Global bytes = 01000100
  - All KATs restored exactly: K_stream 73635f..., block0 6818e6..., block1 ac6a5d..., boundary 021a6c..., uniform_below(10)=7, shuffle->[1,3,4,0,2]

- R2: Redesign RandomStreamKeyV1 with unrepresentable invalid combinations
  - enum RandomStreamScopeV1 { Global, Player(u64) }
  - struct RandomStreamKeyV1 { kind: RandomStreamKindV1, scope: RandomStreamScopeV1 }
  - Codec version 0x01 as encoder constant, not public semantic field
  - Reject unknown version/kind/scope/trailing/malformed bytes

- R3: RandomStateV1 single BTreeMap source of truth
  - Custom serialization explicitly sorts by key.to_canonical_bytes()
  - Deserialize rejects duplicate canonical key bytes before map creation
  - Permit every u64 cursor including u64::MAX
  - Removed NonZeroInitialCursor policy

- R4: Remove duplicate RNG from mtgml-state
  - EngineState delegates to mtgml-random primitives
  - Fixed consumed-word accounting
  - Checked usize->u64 conversion before shuffle
  - Missing stream and initial exhaustion = zero mutation

- R5: EngineState key validation complete
  - Supported key codec, valid kind, valid scope shape
  - Player(p) must reference existing core player

- R6: Replay V2 parity (Rust/Python)
  - ReplayManifestV2.validate mirrors Rust checks
  - ReplayManifestV2.to_wire calls validate
  - JSON Schema sets replay_step const "replay-step.v2"
  - Negative fixtures added for V1/V2 mixing, etc.

- R8: RNG_CONTRACT.md status updated to "not implemented"

All workspace tests pass, check-fast/check/check-all pass.
DETERMINISTIC_RNG_AND_ALLOCATORS remains NOT_RUN per M1.5.